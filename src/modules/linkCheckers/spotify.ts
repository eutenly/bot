import Message from "../../classes/Message/Message";
// import spotifyTrack from "../spotify/track/main";

export default function spotify(input: string): Function | undefined {

    // Check if input is a tweet link
    // const tweet = input.match(/twitter\.com\/(.+)\/status\/(.+)/);
    // if (tweet) return (message: Message) => spotifyTrack(message, tweet[2]);

    return;
}