export default class Websites {

    // The URL for the website
    _id: string;

    // Custom fields for the embed
    fields: [{
        title: string,
        description: string,
        inline: boolean
    }];
};